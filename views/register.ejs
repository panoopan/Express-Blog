<!DOCTYPE html>
<html>
  <head>
    <%- include("template/head")%>
  </head>

  <body>
    <%- include("template/navbar") %>

    <div
      class="col-10 col-md-4 my-5 mx-auto"
      style="
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
      "
    >
      <h1 class="text-center mb-5">Register</h1>
      <section class="error">
        <% if(errMessage && errMessage.length > 0) { %>
        <div class="alert alert-danger" role="alert"><%= errMessage %></div>
        <% } %>
      </section>
      <form
        method="POST"
        action="/register"
        class="shadow-lg p-3 pb-5 mb-5 bg-white rounded"
      >
        <div class="form-group">
          USERNAME
          <input name="username" type="text" class="form-control" />
        </div>
        <div class="form-group">
          PASSWORD
          <input
            name="password"
            type="password"
            class="form-control"
            id="exampleInputPassword1"
          />
        </div>
        <button type="submit" class="btn btn-info float-right">Submit</button>
      </form>
    </div>

    <!--
  <div class="login-wrapper">
    <h2>Register</h2>
    <section class="error">
        <% if(errMessage) { %>
            <h3 class="error"><%= errMessage %></h3>  
        <% } %> 
    </section>
    <form method="POST" action="/register">
        <div class="input__wrapper">
            <div class="input__label">USERNAME</div>
            <input class="input__field" type="text" name="username" />
          </div>
          
          <div class="input__wrapper">
            <div class="input__label">PASSWORD</div>
            <input class="input__field" type="password" name="password" />
          </div>
          <input type='submit' value="註冊" />
    </form>    
  </div>
-->
    <%- include("template/bundle") %>
  </body>
</html>
